# @package _global_
defaults:
  - /experiment/kv14m_wide/kv_pretrain/_base-fa
  - _self_

training:
  wandb_tags: [pretrain, kv, scratch, swa, stateful-swa, swafull-fix-rerun, kv14m-wide]

model:
  seq_modeling_block: SWA
  force_flash: False
  unroll_block_scan: True
  sliding_window_size: 1024
  mini_batch_size: 1024
