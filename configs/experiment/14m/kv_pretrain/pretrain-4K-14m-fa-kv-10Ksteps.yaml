# @package _global_
defaults:
  - 14m/kv_pretrain/_base-fa
  - _self_

training:
  exp_name: pretrain-4K-14m-fa-kv-10Ksteps
  wandb_tags: [pretrain, kv, scratch, fa, 4K, steps-10K]
  seq_length: 4096
  global_batch_size: 256
  accum_steps: 8
  total_steps: 10000
  num_evals: 1

  optimizer_outer:
    lr_decay_steps: 10000
    lr_warmup_steps: 1000

model:
  mini_batch_size: 4096
