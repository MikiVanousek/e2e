# @package _global_
defaults:
  - 14m/kv_pretrain/_base-fa
  - _self_

training:
  exp_name: pretrain-8K-14m-fa-kv-10Ksteps
  wandb_tags: [pretrain, kv, scratch, fa, 8K, steps-10K]
  seq_length: 8192
  global_batch_size: 128
  accum_steps: 8
  total_steps: 10000
  num_evals: 1

  optimizer_outer:
    lr_decay_steps: 10000
    lr_warmup_steps: 1000

model:
  mini_batch_size: 8192
