defaults:
  - backend: gpu
  - model: base_model
  - training: base_training
  - dataset: fineweb-edu-10bt
  - checkpoint: base_checkpoint
  - _self_

checkpoint:
  checkpoint_dir: ${deploy_paths.checkpoint}/${training.exp_folder}/${training.run_name}
  resume_checkpoint_dir: ${deploy_paths.checkpoint}/${training.exp_folder}/${training.resume_exp_name}
